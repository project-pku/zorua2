[workspace]
members = ["zorua_macro"]

[package]
name = "zorua"
version = "0.12.0"
edition = "2024"

[features]
default = ["std"]
std = []

[dependencies]
paste = "^1.0.14"
ux2 = { version = "^0.8.5", features = ["32"] }
zorua_macro = { path = "zorua_macro" }
zerocopy = { version = "0.8.48", default-features = false, features = ["derive"] }

[dev-dependencies]
trybuild = "1"
